apiVersion: v1
kind: Service
metadata:
  name: meikao
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "meikao"
    port: 8080
  selector:
    app: meikao
